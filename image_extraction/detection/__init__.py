import identity
import ocr

__all__ = ['', 'identity']


def find_identity_boxes():
    """
    A function that finds all identity boxes on an image of a homework organizer.
    Boxes included: subject box, name box, and student_id box

    :return: A dictionary containing the coordinates of all the identity boxes in the form of Box objects.
    """


def find_score_boxes():
    """
    A function that finds all score boxes on an image of a homework organizer.

    :return: A 2-D array containing the coordinates of all the score boxes in the form of Box objects.
    """